package data_structure.PSS.pss07;//
//public static void bubbleSort( Position<E> p1, Position<E> p2 ) {
//	for ( Position<E> p = p1; ! p.equals( p2 ); p = p.getNext() ) {
//		boolean swapped = false;
//		// Swap adjacent contents, if necessary
//		for ( Position<E> q = p1.getNext(); ! q.equals( p2 ); q = q.getNext() ) {
//			Position<E> qPrev = q.getPrev();
//			if ( qPrev.getElement() > q.getElement() ) {
//				// Swap content
//				E temp = qPrev.getElement();
//				qPrev.setElement( q.getElement() );
//				q.setElement( temp );
//			}
//		}
//		// If no changes were performed, then already sorted
//		if ( ! swapped )
//			return;
//	}
//}
